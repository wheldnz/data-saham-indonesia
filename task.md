- `[x]` Create database model for portfolio transactions in `backend/app/models/portfolio.py`.
- `[x]` Modify `backend/app/main.py` to:
  - Initialize the new table on startup.
  - Implement `/api/portfolio` GET endpoint (calculates cash, holdings, realized/unrealized PnL, win-rate).
  - Implement `/api/portfolio/trade` POST endpoint (buy/sell trade validation and logging).
  - Implement `/api/reports/daily/csv` GET endpoint (CSV export of EOD predictions).
  - Implement `/api/stocks/compare` GET endpoint (fetch comparative metrics for side-by-side comparison).
  - Update `/api/chart/{ticker}` to return technical features overlays (SMA 5/20/50, Bollinger Bands).
- `[x]` Integrate indicators chart overlays (SMA 5/20/50, Bollinger Bands) in `frontend/src/App.jsx`.
- `[x]` Add Virtual Portfolio & Paper Trading UI tab and trade action modals in `frontend/src/App.jsx`.
- `[x]` Add report CSV export and browser PDF print triggers in `frontend/src/App.jsx`.
- `[x]` Add Side-by-Side Stock Comparison Panel under the Watchlist tab in `frontend/src/App.jsx`.
- `[x]` Verify all endpoints and features using an automated test script and manual UI check.
